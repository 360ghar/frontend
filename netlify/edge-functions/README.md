# Edge functions (active)

This directory is intentionally **empty** after the 2026-07-14 production outage
(site-wide HTTP 301 self-redirect loops).

Parked implementations live in `../edge-functions-disabled/`. Re-enable only
after the checklist in that folder’s README.
